<%#
# HTML Index Partial

This partial renders a HTML attribute
to be displayed on a resource's index page.

By default, the attribute is rendered as plain HTML

## Local variables:

- `field`:
  An instance of Administrate::Field::HTML.
  A wrapper around the html pulled from the database.
%>

<%= sanitize field.data %>
