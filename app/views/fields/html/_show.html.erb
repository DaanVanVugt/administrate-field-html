<%#
# HTML Show Partial

This partial renders an HTML attribute,
to be displayed on a resource's show page.

By default, the attribute is rendered as sanitized HTML

## Local variables:

- `field`:
  An instance of Administrate::Field::HTML.
  A wrapper around the html pulled from the database.
%>

<%= sanitize field.data %>
